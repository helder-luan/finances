import 'package:fingen/src/core/app_sizes.dart';
import 'package:flutter/material.dart';

class AppTextStyles {
  static const TextStyle title = TextStyle(fontSize: AppSizes.textTitle, fontWeight: FontWeight.bold);
  static const TextStyle subtitle = TextStyle(fontSize: AppSizes.textSubtitle);
  static const TextStyle primary = TextStyle(fontSize: AppSizes.textPrimary);
  static const TextStyle secondary = TextStyle(fontSize: AppSizes.textSecondary);
}